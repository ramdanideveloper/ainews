@extends('layout',['title'=>'Dashboard']) @section('content')<h1>Halo, {{ $user->name }}</h1><div class="grid"><div class="card"><span class="muted">Saldo</span><div class="big">Rp{{ number_format($balance,0,',','.') }}</div><p>Top up saat ini diproses manual oleh admin.</p></div><div class="card"><span class="muted">Connected Sites</span><div class="big">{{ $sites->count() }}</div></div><div class="card"><span class="muted">Welcome Credit</span><div class="big">{{ $user->welcome_credit_granted_at ? 'Diterima' : 'Belum' }}</div></div></div><div class="card"><h2>Connected Websites</h2><table><tr><th>Site</th><th>Status</th><th>Token</th><th>Last seen</th></tr>@forelse($sites as $site)<tr><td>{{ $site->site_name }}<br><small>{{ $site->domain }}</small></td><td>{{ $site->status }}</td><td>••••{{ $site->token_last_four }}</td><td>{{ optional($site->last_seen_at)->diffForHumans() }}</td></tr>@empty<tr><td colspan="4">Belum ada site terhubung. Hubungkan dari plugin WordPress.</td></tr>@endforelse</table></div><div class="card"><h2>Usage Terbaru</h2><table><tr><th>Waktu</th><th>Tipe</th><th>Token</th><th>Biaya</th><th>Status</th></tr>@foreach($usage as $row)<tr><td>{{ $row->created_at }}</td><td>{{ $row->request_type }}</td><td>{{ number_format($row->total_tokens) }}</td><td>Rp{{ number_format((float)$row->charged_amount,0,',','.') }}</td><td>{{ $row->status }}</td></tr>@endforeach</table></div><div class="card"><h2>Transaksi</h2><table><tr><th>Waktu</th><th>Tipe</th><th>Jumlah</th><th>Saldo</th></tr>@foreach($transactions as $row)<tr><td>{{ $row->created_at }}</td><td>{{ $row->type }}</td><td>Rp{{ number_format((float)$row->amount,0,',','.') }}</td><td>Rp{{ number_format((float)$row->balance_after,0,',','.') }}</td></tr>@endforeach</table></div>@endsection

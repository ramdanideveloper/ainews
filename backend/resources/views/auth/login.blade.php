@extends('layout',['title'=>'Login']) @section('content')<div class="card auth"><h1>Login</h1><p class="muted">Kelola saldo dan website terhubung.</p>@if($errors->any())<p class="error">{{ $errors->first() }}</p>@endif<form method="post">@csrf<label>Email<input type="email" name="email" value="{{ old('email') }}" required></label><label>Password<input type="password" name="password" required></label><button>Login</button> <a href="/register">Register</a></form></div>@endsection
